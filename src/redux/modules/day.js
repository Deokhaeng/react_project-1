// day.js

// Actions

const initialState = { item: ["월", "화", "수", "목", "금", "토", "일"] };

// Action Creators

// Reducer
export default function reducer(state = initialState) {
  // do reducer stuff
  return state;
}
